### **React JS-specific snippets**
Cover everything you need for modern web development using React.

## Key Categories Included:

### ⚛️ **Component Scaffolding**
- `rfc` – Functional component
- `rfcp` – Functional component with PropTypes
- `rnfc` – Named functional component
- `rfcx` – Component with export at bottom
- `rfcts` – TypeScript component with interface

### 🔁 **React Hooks**
- `ush` – useState hook
- `ueh` – useEffect hook
- `urh` – useRef hook
- `umh` – useMemo hook
- `ucb` – useCallback hook
- `uct` – Custom hook structure

### 🧪 **Debugging & Logs**
- `clg` – Console log
- `clo` – Console log object
- `cer` – Console error
- `cwr` – Console warn
- `ctb` – Console table
- `ctr` – Console trace

### 🌐 **Async / API Handling**
- `asf` – Async function wrapper
- `fet` – Fetch GET with try/catch
- `axg` – Axios GET template
- `axp` – Axios POST template
- `tryc` – Try/catch block
- `prom` – Promise pattern

### 🎯 **Event Handlers**
- `onc` – onClick handler
- `onch` – onChange handler
- `ons` – onSubmit handler
- `onf` – Form handler boilerplate

### 📝 **Forms & Inputs**
- `rfh` – React Hook Form setup
- `rfhc` – Controlled input component
- `formik` – Formik form template
- `yups` – Yup schema

### 🧩 **State Management**
- `reduxs` – Redux slice
- `reduxa` – Redux async thunk
- `ctx` – React context with provider
- `ctxu` – useContext hook with value

### 🧱 **UI / DOM Utilities**
- `mapr` – Array map rendering
- `ifr` – Conditional render
- `lod` – Loading state component
- `errb` – Error boundary
- `port` – Portal modal setup

### 🛠️ **TypeScript Helpers**
- `tsi` – Interface definition
- `tst` – Type definition
- `tse` – Enum template
- `tsp` – Props interface for components

### 🧰 **Common Utilities**
- `af` – Arrow function
- `ter` – Ternary condition
- `imp` – Import statement
- `exd` – Export default
- `exp` – Named export

## Installation:
Open VSCode command palette → “Configure User Snippets” → then paste into the appropriate  
`javascript.json`, `typescript.json`, `javascriptreact.json`, or `typescriptreact.json` file.

These snippets are optimized for fast development and follow React best practices. Happy coding! ⚡
